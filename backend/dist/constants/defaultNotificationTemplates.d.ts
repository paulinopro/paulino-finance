export type NotificationTemplateType = 'CARD_PAYMENT' | 'LOAN_PAYMENT' | 'RECURRING_EXPENSE';
/** Textos por defecto para plantillas por usuario (título + mensaje con variables {nombre}). */
export declare const DEFAULT_NOTIFICATION_TEMPLATES: Record<NotificationTemplateType, {
    title: string;
    message: string;
}>;
//# sourceMappingURL=defaultNotificationTemplates.d.ts.map