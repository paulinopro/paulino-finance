version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: paulino-finance-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-paulino_finance}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - paulino-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: paulino-finance-backend
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT: ${BACKEND_PORT:-5000}
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: ${DB_NAME:-paulino_finance}
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-jwt-key-change-in-production}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      OPENWA_ENABLED: ${OPENWA_ENABLED:-false}
      OPENWA_API_KEY: ${OPENWA_API_KEY:-}
      OPENWA_SESSION_ID: ${OPENWA_SESSION_ID:-paulino-finance-central}
      OPENWA_BASE_URL: ${OPENWA_BASE_URL:-http://openwa:2785/api}
      OPENWA_REQUEST_TIMEOUT_MS: ${OPENWA_REQUEST_TIMEOUT_MS:-10000}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY:-}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY:-}
      VAPID_SUBJECT: ${VAPID_SUBJECT:-mailto:support@localhost}
      # PayPal: Compose sustituye desde el .env de la RAÍZ del repo (no llegan solo con dotenv dentro del contenedor)
      PAYPAL_CLIENT_ID: ${PAYPAL_CLIENT_ID:-}
      PAYPAL_CLIENT_SECRET: ${PAYPAL_CLIENT_SECRET:-}
      PAYPAL_API_BASE: ${PAYPAL_API_BASE:-https://api-m.sandbox.paypal.com}
      PAYPAL_RETURN_URL: ${PAYPAL_RETURN_URL:-}
      PAYPAL_CANCEL_URL: ${PAYPAL_CANCEL_URL:-}
      PAYPAL_WEBHOOK_ID: ${PAYPAL_WEBHOOK_ID:-}
      SUPER_ADMIN_EMAILS: ${SUPER_ADMIN_EMAILS:-}
      # Enlaces absolutos (auth: recuperación de contraseña, etc.). Misma URL pública que usa el navegador.
      FRONTEND_URL: ${FRONTEND_URL:-}
      APP_PUBLIC_URL: ${APP_PUBLIC_URL:-}
      # Orígenes CORS (coma-separados). En dev con front en otro puerto: p. ej. http://localhost:3000
      CORS_ORIGIN: ${CORS_ORIGIN:-}
      # Revisión desplegada (GET /admin/health → deployRef). Opcional.
      RELEASE: ${RELEASE:-}
      GIT_COMMIT: ${GIT_COMMIT:-}
      VERCEL_GIT_COMMIT_SHA: ${VERCEL_GIT_COMMIT_SHA:-}
      RAILWAY_GIT_COMMIT_SHA: ${RAILWAY_GIT_COMMIT_SHA:-}
      COMMIT_SHA: ${COMMIT_SHA:-}
      # Tasa DOP/USD por defecto (nuevos usuarios y fallback si falta en BD); prioridad: users.exchange_rate_dop_usd
      EXCHANGE_RATE_DOP_USD: ${EXCHANGE_RATE_DOP_USD:-}
    ports:
      - "${BACKEND_PORT:-5000}:5000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - paulino-network
    # npm install en cada arranque: el volumen anónimo /app/node_modules no refleja nuevas deps del host
    # (p. ej. web-push) si solo existía node_modules/.bin de una imagen antigua.
    command: sh -c "npm install && exec npm run dev"

  openwa:
    image: ghcr.io/rmyndharis/openwa:0.12.5
    container_name: paulino-finance-openwa
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 2785
      API_MASTER_KEY: ${OPENWA_API_KEY:-}
      AUTO_START_SESSIONS: ${OPENWA_AUTO_START_SESSIONS:-true}
      ENGINE_TYPE: ${OPENWA_ENGINE_TYPE:-whatsapp-web.js}
    ports:
      - "127.0.0.1:${OPENWA_PORT:-2785}:2785"
    volumes:
      - openwa_data:/app/data
    networks:
      - paulino-network
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:2785/api/health/ready').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 60s
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: paulino-finance-frontend
    environment:
      REACT_APP_API_URL: ${REACT_APP_API_URL:-http://localhost:5000/api}
      REACT_APP_ENABLE_PWA: ${REACT_APP_ENABLE_PWA:-true}
      REACT_APP_ENABLE_PUSH_SYNC: ${REACT_APP_ENABLE_PUSH_SYNC:-}
      CHOKIDAR_USEPOLLING: true
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - paulino-network
    stdin_open: true
    tty: true
    # npm install en cada arranque: el volumen anónimo /app/node_modules no refleja nuevas deps del host
    # (p. ej. react-i18next) si solo se instaló cuando existía @craco/craco de una imagen antigua.
    command: sh -c "npm install && exec npm start"

volumes:
  postgres_data:
  openwa_data:

networks:
  paulino-network:
    driver: bridge
